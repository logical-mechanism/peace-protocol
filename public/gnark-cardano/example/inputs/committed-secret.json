{
  "_comment": "Pattern: committed-secret (x*x == y, with Pedersen commitment on x). Only the secret x is supplied; y = x*x is derived inside CircuitProve. The Pedersen commitment is added by the gnark frontend automatically — no extra fields needed in the JSON.",
  "x": "13"
}
