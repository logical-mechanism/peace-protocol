# Dependencies
node_modules/

# Build outputs
dist/
target/

# Environment files
.env
.env.local
.env.*.local

# Tauri
src-tauri/target/
src-tauri/gen/

# Chain data (should only exist in app_data_dir, not source tree)
src-tauri/mainnet/
src-tauri/preprod/

# Sidecar binaries (platform-specific, ~18-100MB each)
src-tauri/binaries/

# SNARK setup files (too large for git)
src-tauri/resources/snark/*.zst
src-tauri/resources/snark/*.bin

# Bundled backend (copied during build, not source)
src-tauri/resources/be/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Test coverage
coverage/

# Package lock files (w
# Tauri
src-tauri/target/
src-tauri/gen/

# Chain data (should only exist in app_data_dir, not source tree)
orkspace uses root lock)
fe/package-lock.json
be/package-lock.json
